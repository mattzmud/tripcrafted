// version.js
// Bump this number any time you update firebase-config.js (or other shared
// files later) and want to force browsers to fetch the fresh copy instead
// of a cached one. Same pattern as Common Print Co's shared/version.js.
export const VERSION = '1.0.5';
